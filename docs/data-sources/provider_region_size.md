---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "laravelforge_provider_region_size Data Source - laravelforge"
subcategory: ""
description: |-
  Fetches a single Laravel Forge provider size available within a specific provider region. Some providers gate which sizes are offered per-region, so this is the region-scoped subset (separate from laravelforge_provider_size). Scoped to a server provider and region, not to an organization.
---

# laravelforge_provider_region_size (Data Source)

Fetches a single Laravel Forge provider size available within a specific provider region. Some providers gate which sizes are offered per-region, so this is the region-scoped subset (separate from `laravelforge_provider_size`). Scoped to a server provider and region, not to an organization.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Forge ID of the provider size.
- `provider_id` (Number) Numeric ID of the server provider that offers the size (Forge API `provider` path segment; `provider` is reserved in HCL).
- `region_id` (Number) Numeric ID of the provider region the size is available in.

### Read-Only

- `name` (String) Human-readable name of the size.
