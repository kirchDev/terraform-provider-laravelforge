---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "laravelforge_server_php_max_upload_size Resource - laravelforge"
subcategory: ""
description: |-
  Manages the PHP max upload size (upload_max_filesize / post_max_size) on a Laravel Forge server. The setting is a singleton on the server (no separate ID): it is read via GET and replaced via PUT, and removing the resource leaves the current value in place (Forge has no destroy endpoint for it).
---

# laravelforge_server_php_max_upload_size (Resource)

Manages the PHP max upload size (`upload_max_filesize` / `post_max_size`) on a Laravel Forge server. The setting is a singleton on the server (no separate ID): it is read via `GET` and replaced via `PUT`, and removing the resource leaves the current value in place (Forge has no destroy endpoint for it).



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `organization` (String) Organization slug.
- `server_id` (Number) ID of the server whose PHP max upload size is managed.

### Optional

- `max_upload_size` (Number) Maximum upload size in megabytes.
